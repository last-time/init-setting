# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions
LC_ALL=zh_CN.utf-8
PATH=${PATH}:/home/chenming-xy/bin
export LC_ALL
export PATH
stty -ixon
eval `dircolors ~/.dir_colors`
